services:
  - type: web
    name: voice-agent-backend
    env: node
    plan: free
    buildCommand: pnpm install --no-frozen-lockfile && pnpm --filter api-server build
    startCommand: node artifacts/api-server/dist/index.js
    envVars:
      - key: NODE_ENV
        value: production